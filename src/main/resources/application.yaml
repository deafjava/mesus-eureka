spring:
  application:
    name: eureka-server

eureka:
  instance:
    hostname: mesus-eureka.herokuapp.com
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enableSelfPreservation: false
